echo "ERROR: set up /etc/cross-shell/config first." >&2
exit 1

# IPlinux archive suite
SUITE=asimov

# IPlinux repository URL
REPO=http://ftp.iplinux.org/iplinux/

# root gaining command (e.g. sudo)
ROOT_CMD=sudo
